[project]
name = "yamly"
version = "0.1.0"
description = "Build GitHub Actions workflows from templates and export production-ready YAML in seconds"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.56.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
